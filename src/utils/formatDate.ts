// format date (entire file)


// return last character of date string
    // change 3 to 3rd, 4 to 4th, ect

// function to format timestamp 
export const dateFormat = async(
    //timestamp: any,

) => {
    // format months to three letter suffix ex (Jan, Feb)
    //const dateObj = new Date(timestamp)
    return;
}